package naturacert.baumsoft.dev.naturacert;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import naturacert.baumsoft.dev.naturacert.p3p1;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table P3P1.
*/
public class p3p1Dao extends AbstractDao<p3p1, Long> {

    public static final String TABLENAME = "P3P1";

    /**
     * Properties of entity p3p1.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Cmivs = new Property(1, Integer.class, "cmivs", false, "CMIVS");
        public final static Property Ivs = new Property(2, Integer.class, "ivs", false, "IVS");
        public final static Property Idh = new Property(3, Integer.class, "idh", false, "IDH");
        public final static Property Origen = new Property(4, Integer.class, "origen", false, "ORIGEN");
        public final static Property Obser = new Property(5, String.class, "obser", false, "OBSER");
        public final static Property Id_formulario = new Property(6, Integer.class, "id_formulario", false, "ID_FORMULARIO");
    };


    public p3p1Dao(DaoConfig config) {
        super(config);
    }
    
    public p3p1Dao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'P3P1' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'CMIVS' INTEGER," + // 1: cmivs
                "'IVS' INTEGER," + // 2: ivs
                "'IDH' INTEGER," + // 3: idh
                "'ORIGEN' INTEGER," + // 4: origen
                "'OBSER' TEXT," + // 5: obser
                "'ID_FORMULARIO' INTEGER);"); // 6: id_formulario
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'P3P1'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, p3p1 entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer cmivs = entity.getCmivs();
        if (cmivs != null) {
            stmt.bindLong(2, cmivs);
        }
 
        Integer ivs = entity.getIvs();
        if (ivs != null) {
            stmt.bindLong(3, ivs);
        }
 
        Integer idh = entity.getIdh();
        if (idh != null) {
            stmt.bindLong(4, idh);
        }
 
        Integer origen = entity.getOrigen();
        if (origen != null) {
            stmt.bindLong(5, origen);
        }
 
        String obser = entity.getObser();
        if (obser != null) {
            stmt.bindString(6, obser);
        }
 
        Integer id_formulario = entity.getId_formulario();
        if (id_formulario != null) {
            stmt.bindLong(7, id_formulario);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public p3p1 readEntity(Cursor cursor, int offset) {
        p3p1 entity = new p3p1( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // cmivs
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // ivs
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3), // idh
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // origen
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // obser
            cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6) // id_formulario
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, p3p1 entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setCmivs(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setIvs(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setIdh(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
        entity.setOrigen(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setObser(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setId_formulario(cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(p3p1 entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(p3p1 entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
