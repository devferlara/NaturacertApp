package naturacert.baumsoft.dev.naturacert;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import naturacert.baumsoft.dev.naturacert.p4p9;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table P4P9.
*/
public class p4p9Dao extends AbstractDao<p4p9, Long> {

    public static final String TABLENAME = "P4P9";

    /**
     * Properties of entity p4p9.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Fcpr = new Property(1, Integer.class, "fcpr", false, "FCPR");
        public final static Property Pmas = new Property(2, Integer.class, "pmas", false, "PMAS");
        public final static Property Frec = new Property(3, Integer.class, "frec", false, "FREC");
        public final static Property Anar = new Property(4, Integer.class, "anar", false, "ANAR");
        public final static Property Pmue = new Property(5, Integer.class, "pmue", false, "PMUE");
        public final static Property Amdc = new Property(6, Integer.class, "amdc", false, "AMDC");
        public final static Property Origen = new Property(7, Integer.class, "origen", false, "ORIGEN");
        public final static Property Id_formulario = new Property(8, Integer.class, "id_formulario", false, "ID_FORMULARIO");
        public final static Property Observaciones = new Property(9, String.class, "observaciones", false, "OBSERVACIONES");
    };


    public p4p9Dao(DaoConfig config) {
        super(config);
    }
    
    public p4p9Dao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'P4P9' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'FCPR' INTEGER," + // 1: fcpr
                "'PMAS' INTEGER," + // 2: pmas
                "'FREC' INTEGER," + // 3: frec
                "'ANAR' INTEGER," + // 4: anar
                "'PMUE' INTEGER," + // 5: pmue
                "'AMDC' INTEGER," + // 6: amdc
                "'ORIGEN' INTEGER," + // 7: origen
                "'ID_FORMULARIO' INTEGER," + // 8: id_formulario
                "'OBSERVACIONES' TEXT);"); // 9: observaciones
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'P4P9'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, p4p9 entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer fcpr = entity.getFcpr();
        if (fcpr != null) {
            stmt.bindLong(2, fcpr);
        }
 
        Integer pmas = entity.getPmas();
        if (pmas != null) {
            stmt.bindLong(3, pmas);
        }
 
        Integer frec = entity.getFrec();
        if (frec != null) {
            stmt.bindLong(4, frec);
        }
 
        Integer anar = entity.getAnar();
        if (anar != null) {
            stmt.bindLong(5, anar);
        }
 
        Integer pmue = entity.getPmue();
        if (pmue != null) {
            stmt.bindLong(6, pmue);
        }
 
        Integer amdc = entity.getAmdc();
        if (amdc != null) {
            stmt.bindLong(7, amdc);
        }
 
        Integer origen = entity.getOrigen();
        if (origen != null) {
            stmt.bindLong(8, origen);
        }
 
        Integer id_formulario = entity.getId_formulario();
        if (id_formulario != null) {
            stmt.bindLong(9, id_formulario);
        }
 
        String observaciones = entity.getObservaciones();
        if (observaciones != null) {
            stmt.bindString(10, observaciones);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public p4p9 readEntity(Cursor cursor, int offset) {
        p4p9 entity = new p4p9( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // fcpr
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // pmas
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3), // frec
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // anar
            cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5), // pmue
            cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6), // amdc
            cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7), // origen
            cursor.isNull(offset + 8) ? null : cursor.getInt(offset + 8), // id_formulario
            cursor.isNull(offset + 9) ? null : cursor.getString(offset + 9) // observaciones
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, p4p9 entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setFcpr(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setPmas(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setFrec(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
        entity.setAnar(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setPmue(cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5));
        entity.setAmdc(cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6));
        entity.setOrigen(cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7));
        entity.setId_formulario(cursor.isNull(offset + 8) ? null : cursor.getInt(offset + 8));
        entity.setObservaciones(cursor.isNull(offset + 9) ? null : cursor.getString(offset + 9));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(p4p9 entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(p4p9 entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
