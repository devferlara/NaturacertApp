package naturacert.baumsoft.dev.naturacert;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import naturacert.baumsoft.dev.naturacert.p8p9;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table P8P9.
*/
public class p8p9Dao extends AbstractDao<p8p9, Long> {

    public static final String TABLENAME = "P8P9";

    /**
     * Properties of entity p8p9.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Sspu = new Property(1, Integer.class, "sspu", false, "SSPU");
        public final static Property Eeud = new Property(2, Integer.class, "eeud", false, "EEUD");
        public final static Property Eudf = new Property(3, Integer.class, "eudf", false, "EUDF");
        public final static Property Fsub = new Property(4, Integer.class, "fsub", false, "FSUB");
        public final static Property Origen = new Property(5, Integer.class, "origen", false, "ORIGEN");
        public final static Property Id_formulario = new Property(6, Integer.class, "id_formulario", false, "ID_FORMULARIO");
        public final static Property Observaciones = new Property(7, String.class, "observaciones", false, "OBSERVACIONES");
    };


    public p8p9Dao(DaoConfig config) {
        super(config);
    }
    
    public p8p9Dao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'P8P9' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'SSPU' INTEGER," + // 1: sspu
                "'EEUD' INTEGER," + // 2: eeud
                "'EUDF' INTEGER," + // 3: eudf
                "'FSUB' INTEGER," + // 4: fsub
                "'ORIGEN' INTEGER," + // 5: origen
                "'ID_FORMULARIO' INTEGER," + // 6: id_formulario
                "'OBSERVACIONES' TEXT);"); // 7: observaciones
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'P8P9'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, p8p9 entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer sspu = entity.getSspu();
        if (sspu != null) {
            stmt.bindLong(2, sspu);
        }
 
        Integer eeud = entity.getEeud();
        if (eeud != null) {
            stmt.bindLong(3, eeud);
        }
 
        Integer eudf = entity.getEudf();
        if (eudf != null) {
            stmt.bindLong(4, eudf);
        }
 
        Integer fsub = entity.getFsub();
        if (fsub != null) {
            stmt.bindLong(5, fsub);
        }
 
        Integer origen = entity.getOrigen();
        if (origen != null) {
            stmt.bindLong(6, origen);
        }
 
        Integer id_formulario = entity.getId_formulario();
        if (id_formulario != null) {
            stmt.bindLong(7, id_formulario);
        }
 
        String observaciones = entity.getObservaciones();
        if (observaciones != null) {
            stmt.bindString(8, observaciones);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public p8p9 readEntity(Cursor cursor, int offset) {
        p8p9 entity = new p8p9( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // sspu
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // eeud
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3), // eudf
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // fsub
            cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5), // origen
            cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6), // id_formulario
            cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7) // observaciones
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, p8p9 entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setSspu(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setEeud(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setEudf(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
        entity.setFsub(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setOrigen(cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5));
        entity.setId_formulario(cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6));
        entity.setObservaciones(cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(p8p9 entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(p8p9 entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
