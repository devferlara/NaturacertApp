package naturacert.baumsoft.dev.naturacert;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import naturacert.baumsoft.dev.naturacert.p1p8;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table P1P8.
*/
public class p1p8Dao extends AbstractDao<p1p8, Long> {

    public static final String TABLENAME = "P1P8";

    /**
     * Properties of entity p1p8.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Psfc = new Property(1, Integer.class, "psfc", false, "PSFC");
        public final static Property Mep = new Property(2, Integer.class, "mep", false, "MEP");
        public final static Property Vcp = new Property(3, Integer.class, "vcp", false, "VCP");
        public final static Property Fvc = new Property(4, Integer.class, "fvc", false, "FVC");
        public final static Property Origen = new Property(5, Integer.class, "origen", false, "ORIGEN");
        public final static Property Obser = new Property(6, String.class, "obser", false, "OBSER");
        public final static Property Id_formulario = new Property(7, Integer.class, "id_formulario", false, "ID_FORMULARIO");
    };


    public p1p8Dao(DaoConfig config) {
        super(config);
    }
    
    public p1p8Dao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'P1P8' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'PSFC' INTEGER," + // 1: psfc
                "'MEP' INTEGER," + // 2: mep
                "'VCP' INTEGER," + // 3: vcp
                "'FVC' INTEGER," + // 4: fvc
                "'ORIGEN' INTEGER," + // 5: origen
                "'OBSER' TEXT," + // 6: obser
                "'ID_FORMULARIO' INTEGER);"); // 7: id_formulario
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'P1P8'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, p1p8 entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer psfc = entity.getPsfc();
        if (psfc != null) {
            stmt.bindLong(2, psfc);
        }
 
        Integer mep = entity.getMep();
        if (mep != null) {
            stmt.bindLong(3, mep);
        }
 
        Integer vcp = entity.getVcp();
        if (vcp != null) {
            stmt.bindLong(4, vcp);
        }
 
        Integer fvc = entity.getFvc();
        if (fvc != null) {
            stmt.bindLong(5, fvc);
        }
 
        Integer origen = entity.getOrigen();
        if (origen != null) {
            stmt.bindLong(6, origen);
        }
 
        String obser = entity.getObser();
        if (obser != null) {
            stmt.bindString(7, obser);
        }
 
        Integer id_formulario = entity.getId_formulario();
        if (id_formulario != null) {
            stmt.bindLong(8, id_formulario);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public p1p8 readEntity(Cursor cursor, int offset) {
        p1p8 entity = new p1p8( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // psfc
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // mep
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3), // vcp
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // fvc
            cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5), // origen
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6), // obser
            cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7) // id_formulario
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, p1p8 entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setPsfc(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setMep(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setVcp(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
        entity.setFvc(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setOrigen(cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5));
        entity.setObser(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
        entity.setId_formulario(cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(p1p8 entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(p1p8 entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
