package naturacert.baumsoft.dev.naturacert;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import naturacert.baumsoft.dev.naturacert.p7p3;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table P7P3.
*/
public class p7p3Dao extends AbstractDao<p7p3, Long> {

    public static final String TABLENAME = "P7P3";

    /**
     * Properties of entity p7p3.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Pypp = new Property(1, Integer.class, "pypp", false, "PYPP");
        public final static Property Ccpp = new Property(2, Integer.class, "ccpp", false, "CCPP");
        public final static Property Sccp = new Property(3, Integer.class, "sccp", false, "SCCP");
        public final static Property Lfcp = new Property(4, Integer.class, "lfcp", false, "LFCP");
        public final static Property Lfas = new Property(5, Integer.class, "lfas", false, "LFAS");
        public final static Property Origen = new Property(6, Integer.class, "origen", false, "ORIGEN");
        public final static Property Id_formulario = new Property(7, Integer.class, "id_formulario", false, "ID_FORMULARIO");
        public final static Property Observaciones = new Property(8, String.class, "observaciones", false, "OBSERVACIONES");
    };


    public p7p3Dao(DaoConfig config) {
        super(config);
    }
    
    public p7p3Dao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'P7P3' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'PYPP' INTEGER," + // 1: pypp
                "'CCPP' INTEGER," + // 2: ccpp
                "'SCCP' INTEGER," + // 3: sccp
                "'LFCP' INTEGER," + // 4: lfcp
                "'LFAS' INTEGER," + // 5: lfas
                "'ORIGEN' INTEGER," + // 6: origen
                "'ID_FORMULARIO' INTEGER," + // 7: id_formulario
                "'OBSERVACIONES' TEXT);"); // 8: observaciones
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'P7P3'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, p7p3 entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer pypp = entity.getPypp();
        if (pypp != null) {
            stmt.bindLong(2, pypp);
        }
 
        Integer ccpp = entity.getCcpp();
        if (ccpp != null) {
            stmt.bindLong(3, ccpp);
        }
 
        Integer sccp = entity.getSccp();
        if (sccp != null) {
            stmt.bindLong(4, sccp);
        }
 
        Integer lfcp = entity.getLfcp();
        if (lfcp != null) {
            stmt.bindLong(5, lfcp);
        }
 
        Integer lfas = entity.getLfas();
        if (lfas != null) {
            stmt.bindLong(6, lfas);
        }
 
        Integer origen = entity.getOrigen();
        if (origen != null) {
            stmt.bindLong(7, origen);
        }
 
        Integer id_formulario = entity.getId_formulario();
        if (id_formulario != null) {
            stmt.bindLong(8, id_formulario);
        }
 
        String observaciones = entity.getObservaciones();
        if (observaciones != null) {
            stmt.bindString(9, observaciones);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public p7p3 readEntity(Cursor cursor, int offset) {
        p7p3 entity = new p7p3( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // pypp
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // ccpp
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3), // sccp
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // lfcp
            cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5), // lfas
            cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6), // origen
            cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7), // id_formulario
            cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8) // observaciones
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, p7p3 entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setPypp(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setCcpp(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setSccp(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
        entity.setLfcp(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setLfas(cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5));
        entity.setOrigen(cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6));
        entity.setId_formulario(cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7));
        entity.setObservaciones(cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(p7p3 entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(p7p3 entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
