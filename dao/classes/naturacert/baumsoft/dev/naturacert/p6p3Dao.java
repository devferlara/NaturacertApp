package naturacert.baumsoft.dev.naturacert;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import naturacert.baumsoft.dev.naturacert.p6p3;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table P6P3.
*/
public class p6p3Dao extends AbstractDao<p6p3, Long> {

    public static final String TABLENAME = "P6P3";

    /**
     * Properties of entity p6p3.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Nhtq = new Property(1, Integer.class, "nhtq", false, "NHTQ");
        public final static Property Sice = new Property(2, Integer.class, "sice", false, "SICE");
        public final static Property Cipp = new Property(3, Integer.class, "cipp", false, "CIPP");
        public final static Property Eptl = new Property(4, Integer.class, "eptl", false, "EPTL");
        public final static Property Fdlu = new Property(5, String.class, "fdlu", false, "FDLU");
        public final static Property Origen = new Property(6, Integer.class, "origen", false, "ORIGEN");
        public final static Property Id_formulario = new Property(7, Integer.class, "id_formulario", false, "ID_FORMULARIO");
        public final static Property Observaciones = new Property(8, String.class, "observaciones", false, "OBSERVACIONES");
    };


    public p6p3Dao(DaoConfig config) {
        super(config);
    }
    
    public p6p3Dao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'P6P3' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'NHTQ' INTEGER," + // 1: nhtq
                "'SICE' INTEGER," + // 2: sice
                "'CIPP' INTEGER," + // 3: cipp
                "'EPTL' INTEGER," + // 4: eptl
                "'FDLU' TEXT," + // 5: fdlu
                "'ORIGEN' INTEGER," + // 6: origen
                "'ID_FORMULARIO' INTEGER," + // 7: id_formulario
                "'OBSERVACIONES' TEXT);"); // 8: observaciones
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'P6P3'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, p6p3 entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer nhtq = entity.getNhtq();
        if (nhtq != null) {
            stmt.bindLong(2, nhtq);
        }
 
        Integer sice = entity.getSice();
        if (sice != null) {
            stmt.bindLong(3, sice);
        }
 
        Integer cipp = entity.getCipp();
        if (cipp != null) {
            stmt.bindLong(4, cipp);
        }
 
        Integer eptl = entity.getEptl();
        if (eptl != null) {
            stmt.bindLong(5, eptl);
        }
 
        String fdlu = entity.getFdlu();
        if (fdlu != null) {
            stmt.bindString(6, fdlu);
        }
 
        Integer origen = entity.getOrigen();
        if (origen != null) {
            stmt.bindLong(7, origen);
        }
 
        Integer id_formulario = entity.getId_formulario();
        if (id_formulario != null) {
            stmt.bindLong(8, id_formulario);
        }
 
        String observaciones = entity.getObservaciones();
        if (observaciones != null) {
            stmt.bindString(9, observaciones);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public p6p3 readEntity(Cursor cursor, int offset) {
        p6p3 entity = new p6p3( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // nhtq
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // sice
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3), // cipp
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // eptl
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // fdlu
            cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6), // origen
            cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7), // id_formulario
            cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8) // observaciones
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, p6p3 entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setNhtq(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setSice(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setCipp(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
        entity.setEptl(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setFdlu(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setOrigen(cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6));
        entity.setId_formulario(cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7));
        entity.setObservaciones(cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(p6p3 entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(p6p3 entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
