package naturacert.baumsoft.dev.naturacert;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import naturacert.baumsoft.dev.naturacert.p6p6;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table P6P6.
*/
public class p6p6Dao extends AbstractDao<p6p6, Long> {

    public static final String TABLENAME = "P6P6";

    /**
     * Properties of entity p6p6.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Tltq = new Property(1, Integer.class, "tltq", false, "TLTQ");
        public final static Property Spal = new Property(2, Integer.class, "spal", false, "SPAL");
        public final static Property Spfp = new Property(3, Integer.class, "spfp", false, "SPFP");
        public final static Property Scal = new Property(4, Integer.class, "scal", false, "SCAL");
        public final static Property Ssed = new Property(5, Integer.class, "ssed", false, "SSED");
        public final static Property Seeu = new Property(6, Integer.class, "seeu", false, "SEEU");
        public final static Property Origen = new Property(7, Integer.class, "origen", false, "ORIGEN");
        public final static Property Obser = new Property(8, String.class, "obser", false, "OBSER");
        public final static Property Id_formulario = new Property(9, Integer.class, "id_formulario", false, "ID_FORMULARIO");
    };


    public p6p6Dao(DaoConfig config) {
        super(config);
    }
    
    public p6p6Dao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'P6P6' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'TLTQ' INTEGER," + // 1: tltq
                "'SPAL' INTEGER," + // 2: spal
                "'SPFP' INTEGER," + // 3: spfp
                "'SCAL' INTEGER," + // 4: scal
                "'SSED' INTEGER," + // 5: ssed
                "'SEEU' INTEGER," + // 6: seeu
                "'ORIGEN' INTEGER," + // 7: origen
                "'OBSER' TEXT," + // 8: obser
                "'ID_FORMULARIO' INTEGER);"); // 9: id_formulario
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'P6P6'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, p6p6 entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer tltq = entity.getTltq();
        if (tltq != null) {
            stmt.bindLong(2, tltq);
        }
 
        Integer spal = entity.getSpal();
        if (spal != null) {
            stmt.bindLong(3, spal);
        }
 
        Integer spfp = entity.getSpfp();
        if (spfp != null) {
            stmt.bindLong(4, spfp);
        }
 
        Integer scal = entity.getScal();
        if (scal != null) {
            stmt.bindLong(5, scal);
        }
 
        Integer ssed = entity.getSsed();
        if (ssed != null) {
            stmt.bindLong(6, ssed);
        }
 
        Integer seeu = entity.getSeeu();
        if (seeu != null) {
            stmt.bindLong(7, seeu);
        }
 
        Integer origen = entity.getOrigen();
        if (origen != null) {
            stmt.bindLong(8, origen);
        }
 
        String obser = entity.getObser();
        if (obser != null) {
            stmt.bindString(9, obser);
        }
 
        Integer id_formulario = entity.getId_formulario();
        if (id_formulario != null) {
            stmt.bindLong(10, id_formulario);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public p6p6 readEntity(Cursor cursor, int offset) {
        p6p6 entity = new p6p6( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // tltq
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // spal
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3), // spfp
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // scal
            cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5), // ssed
            cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6), // seeu
            cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7), // origen
            cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8), // obser
            cursor.isNull(offset + 9) ? null : cursor.getInt(offset + 9) // id_formulario
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, p6p6 entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setTltq(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setSpal(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setSpfp(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
        entity.setScal(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setSsed(cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5));
        entity.setSeeu(cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6));
        entity.setOrigen(cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7));
        entity.setObser(cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8));
        entity.setId_formulario(cursor.isNull(offset + 9) ? null : cursor.getInt(offset + 9));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(p6p6 entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(p6p6 entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
