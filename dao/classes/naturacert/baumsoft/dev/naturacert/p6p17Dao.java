package naturacert.baumsoft.dev.naturacert;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import naturacert.baumsoft.dev.naturacert.p6p17;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table P6P17.
*/
public class p6p17Dao extends AbstractDao<p6p17, Long> {

    public static final String TABLENAME = "P6P17";

    /**
     * Properties of entity p6p17.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Pnms = new Property(1, Integer.class, "pnms", false, "PNMS");
        public final static Property Pdms = new Property(2, Integer.class, "pdms", false, "PDMS");
        public final static Property Euac = new Property(3, Integer.class, "euac", false, "EUAC");
        public final static Property Nepl = new Property(4, Integer.class, "nepl", false, "NEPL");
        public final static Property Origen = new Property(5, Integer.class, "origen", false, "ORIGEN");
        public final static Property Obser = new Property(6, String.class, "obser", false, "OBSER");
        public final static Property Id_formulario = new Property(7, Integer.class, "id_formulario", false, "ID_FORMULARIO");
    };


    public p6p17Dao(DaoConfig config) {
        super(config);
    }
    
    public p6p17Dao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'P6P17' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'PNMS' INTEGER," + // 1: pnms
                "'PDMS' INTEGER," + // 2: pdms
                "'EUAC' INTEGER," + // 3: euac
                "'NEPL' INTEGER," + // 4: nepl
                "'ORIGEN' INTEGER," + // 5: origen
                "'OBSER' TEXT," + // 6: obser
                "'ID_FORMULARIO' INTEGER);"); // 7: id_formulario
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'P6P17'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, p6p17 entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer pnms = entity.getPnms();
        if (pnms != null) {
            stmt.bindLong(2, pnms);
        }
 
        Integer pdms = entity.getPdms();
        if (pdms != null) {
            stmt.bindLong(3, pdms);
        }
 
        Integer euac = entity.getEuac();
        if (euac != null) {
            stmt.bindLong(4, euac);
        }
 
        Integer nepl = entity.getNepl();
        if (nepl != null) {
            stmt.bindLong(5, nepl);
        }
 
        Integer origen = entity.getOrigen();
        if (origen != null) {
            stmt.bindLong(6, origen);
        }
 
        String obser = entity.getObser();
        if (obser != null) {
            stmt.bindString(7, obser);
        }
 
        Integer id_formulario = entity.getId_formulario();
        if (id_formulario != null) {
            stmt.bindLong(8, id_formulario);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public p6p17 readEntity(Cursor cursor, int offset) {
        p6p17 entity = new p6p17( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // pnms
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // pdms
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3), // euac
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // nepl
            cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5), // origen
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6), // obser
            cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7) // id_formulario
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, p6p17 entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setPnms(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setPdms(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setEuac(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
        entity.setNepl(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setOrigen(cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5));
        entity.setObser(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
        entity.setId_formulario(cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(p6p17 entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(p6p17 entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
