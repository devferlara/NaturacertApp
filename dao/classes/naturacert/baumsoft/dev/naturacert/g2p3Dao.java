package naturacert.baumsoft.dev.naturacert;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import naturacert.baumsoft.dev.naturacert.g2p3;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table G2P3.
*/
public class g2p3Dao extends AbstractDao<g2p3, Long> {

    public static final String TABLENAME = "G2P3";

    /**
     * Properties of entity g2p3.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Egdt = new Property(1, Integer.class, "egdt", false, "EGDT");
        public final static Property Proc = new Property(2, Integer.class, "proc", false, "PROC");
        public final static Property Lmdg = new Property(3, Integer.class, "lmdg", false, "LMDG");
        public final static Property Origen = new Property(4, Integer.class, "origen", false, "ORIGEN");
        public final static Property Obser = new Property(5, String.class, "obser", false, "OBSER");
        public final static Property Id_formulario = new Property(6, Integer.class, "id_formulario", false, "ID_FORMULARIO");
    };


    public g2p3Dao(DaoConfig config) {
        super(config);
    }
    
    public g2p3Dao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'G2P3' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'EGDT' INTEGER," + // 1: egdt
                "'PROC' INTEGER," + // 2: proc
                "'LMDG' INTEGER," + // 3: lmdg
                "'ORIGEN' INTEGER," + // 4: origen
                "'OBSER' TEXT," + // 5: obser
                "'ID_FORMULARIO' INTEGER);"); // 6: id_formulario
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'G2P3'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, g2p3 entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer egdt = entity.getEgdt();
        if (egdt != null) {
            stmt.bindLong(2, egdt);
        }
 
        Integer proc = entity.getProc();
        if (proc != null) {
            stmt.bindLong(3, proc);
        }
 
        Integer lmdg = entity.getLmdg();
        if (lmdg != null) {
            stmt.bindLong(4, lmdg);
        }
 
        Integer origen = entity.getOrigen();
        if (origen != null) {
            stmt.bindLong(5, origen);
        }
 
        String obser = entity.getObser();
        if (obser != null) {
            stmt.bindString(6, obser);
        }
 
        Integer id_formulario = entity.getId_formulario();
        if (id_formulario != null) {
            stmt.bindLong(7, id_formulario);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public g2p3 readEntity(Cursor cursor, int offset) {
        g2p3 entity = new g2p3( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // egdt
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // proc
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3), // lmdg
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // origen
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // obser
            cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6) // id_formulario
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, g2p3 entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setEgdt(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setProc(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setLmdg(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
        entity.setOrigen(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setObser(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setId_formulario(cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(g2p3 entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(g2p3 entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
