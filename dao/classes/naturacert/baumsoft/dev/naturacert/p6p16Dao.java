package naturacert.baumsoft.dev.naturacert;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import naturacert.baumsoft.dev.naturacert.p6p16;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table P6P16.
*/
public class p6p16Dao extends AbstractDao<p6p16, Long> {

    public static final String TABLENAME = "P6P16";

    /**
     * Properties of entity p6p16.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Elfd = new Property(1, Integer.class, "elfd", false, "ELFD");
        public final static Property Dcdr = new Property(2, Integer.class, "dcdr", false, "DCDR");
        public final static Property Eaes = new Property(3, Integer.class, "eaes", false, "EAES");
        public final static Property Edyv = new Property(4, Integer.class, "edyv", false, "EDYV");
        public final static Property Origen = new Property(5, Integer.class, "origen", false, "ORIGEN");
        public final static Property Id_formulario = new Property(6, Integer.class, "id_formulario", false, "ID_FORMULARIO");
        public final static Property Observaciones = new Property(7, String.class, "observaciones", false, "OBSERVACIONES");
    };


    public p6p16Dao(DaoConfig config) {
        super(config);
    }
    
    public p6p16Dao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'P6P16' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'ELFD' INTEGER," + // 1: elfd
                "'DCDR' INTEGER," + // 2: dcdr
                "'EAES' INTEGER," + // 3: eaes
                "'EDYV' INTEGER," + // 4: edyv
                "'ORIGEN' INTEGER," + // 5: origen
                "'ID_FORMULARIO' INTEGER," + // 6: id_formulario
                "'OBSERVACIONES' TEXT);"); // 7: observaciones
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'P6P16'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, p6p16 entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer elfd = entity.getElfd();
        if (elfd != null) {
            stmt.bindLong(2, elfd);
        }
 
        Integer dcdr = entity.getDcdr();
        if (dcdr != null) {
            stmt.bindLong(3, dcdr);
        }
 
        Integer eaes = entity.getEaes();
        if (eaes != null) {
            stmt.bindLong(4, eaes);
        }
 
        Integer edyv = entity.getEdyv();
        if (edyv != null) {
            stmt.bindLong(5, edyv);
        }
 
        Integer origen = entity.getOrigen();
        if (origen != null) {
            stmt.bindLong(6, origen);
        }
 
        Integer id_formulario = entity.getId_formulario();
        if (id_formulario != null) {
            stmt.bindLong(7, id_formulario);
        }
 
        String observaciones = entity.getObservaciones();
        if (observaciones != null) {
            stmt.bindString(8, observaciones);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public p6p16 readEntity(Cursor cursor, int offset) {
        p6p16 entity = new p6p16( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // elfd
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // dcdr
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3), // eaes
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // edyv
            cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5), // origen
            cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6), // id_formulario
            cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7) // observaciones
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, p6p16 entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setElfd(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setDcdr(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setEaes(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
        entity.setEdyv(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setOrigen(cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5));
        entity.setId_formulario(cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6));
        entity.setObservaciones(cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(p6p16 entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(p6p16 entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
