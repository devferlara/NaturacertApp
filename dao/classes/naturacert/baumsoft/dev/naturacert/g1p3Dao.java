package naturacert.baumsoft.dev.naturacert;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import naturacert.baumsoft.dev.naturacert.g1p3;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table G1P3.
*/
public class g1p3Dao extends AbstractDao<g1p3, Long> {

    public static final String TABLENAME = "G1P3";

    /**
     * Properties of entity g1p3.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Eadg = new Property(1, Integer.class, "eadg", false, "EADG");
        public final static Property Cali = new Property(2, Integer.class, "cali", false, "CALI");
        public final static Property Resu = new Property(3, Integer.class, "resu", false, "RESU");
        public final static Property Fech = new Property(4, Integer.class, "fech", false, "FECH");
        public final static Property Nomb = new Property(5, Integer.class, "nomb", false, "NOMB");
        public final static Property Noca = new Property(6, Integer.class, "noca", false, "NOCA");
        public final static Property Ubic = new Property(7, Integer.class, "ubic", false, "UBIC");
        public final static Property Firm = new Property(8, Integer.class, "firm", false, "FIRM");
        public final static Property Origen = new Property(9, Integer.class, "origen", false, "ORIGEN");
        public final static Property Obser = new Property(10, String.class, "obser", false, "OBSER");
        public final static Property Id_formulario = new Property(11, Integer.class, "id_formulario", false, "ID_FORMULARIO");
    };


    public g1p3Dao(DaoConfig config) {
        super(config);
    }
    
    public g1p3Dao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'G1P3' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'EADG' INTEGER," + // 1: eadg
                "'CALI' INTEGER," + // 2: cali
                "'RESU' INTEGER," + // 3: resu
                "'FECH' INTEGER," + // 4: fech
                "'NOMB' INTEGER," + // 5: nomb
                "'NOCA' INTEGER," + // 6: noca
                "'UBIC' INTEGER," + // 7: ubic
                "'FIRM' INTEGER," + // 8: firm
                "'ORIGEN' INTEGER," + // 9: origen
                "'OBSER' TEXT," + // 10: obser
                "'ID_FORMULARIO' INTEGER);"); // 11: id_formulario
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'G1P3'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, g1p3 entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer eadg = entity.getEadg();
        if (eadg != null) {
            stmt.bindLong(2, eadg);
        }
 
        Integer cali = entity.getCali();
        if (cali != null) {
            stmt.bindLong(3, cali);
        }
 
        Integer resu = entity.getResu();
        if (resu != null) {
            stmt.bindLong(4, resu);
        }
 
        Integer fech = entity.getFech();
        if (fech != null) {
            stmt.bindLong(5, fech);
        }
 
        Integer nomb = entity.getNomb();
        if (nomb != null) {
            stmt.bindLong(6, nomb);
        }
 
        Integer noca = entity.getNoca();
        if (noca != null) {
            stmt.bindLong(7, noca);
        }
 
        Integer ubic = entity.getUbic();
        if (ubic != null) {
            stmt.bindLong(8, ubic);
        }
 
        Integer firm = entity.getFirm();
        if (firm != null) {
            stmt.bindLong(9, firm);
        }
 
        Integer origen = entity.getOrigen();
        if (origen != null) {
            stmt.bindLong(10, origen);
        }
 
        String obser = entity.getObser();
        if (obser != null) {
            stmt.bindString(11, obser);
        }
 
        Integer id_formulario = entity.getId_formulario();
        if (id_formulario != null) {
            stmt.bindLong(12, id_formulario);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public g1p3 readEntity(Cursor cursor, int offset) {
        g1p3 entity = new g1p3( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // eadg
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // cali
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3), // resu
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // fech
            cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5), // nomb
            cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6), // noca
            cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7), // ubic
            cursor.isNull(offset + 8) ? null : cursor.getInt(offset + 8), // firm
            cursor.isNull(offset + 9) ? null : cursor.getInt(offset + 9), // origen
            cursor.isNull(offset + 10) ? null : cursor.getString(offset + 10), // obser
            cursor.isNull(offset + 11) ? null : cursor.getInt(offset + 11) // id_formulario
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, g1p3 entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setEadg(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setCali(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setResu(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
        entity.setFech(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setNomb(cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5));
        entity.setNoca(cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6));
        entity.setUbic(cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7));
        entity.setFirm(cursor.isNull(offset + 8) ? null : cursor.getInt(offset + 8));
        entity.setOrigen(cursor.isNull(offset + 9) ? null : cursor.getInt(offset + 9));
        entity.setObser(cursor.isNull(offset + 10) ? null : cursor.getString(offset + 10));
        entity.setId_formulario(cursor.isNull(offset + 11) ? null : cursor.getInt(offset + 11));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(g1p3 entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(g1p3 entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
