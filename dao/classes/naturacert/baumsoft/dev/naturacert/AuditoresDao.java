package naturacert.baumsoft.dev.naturacert;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;

import naturacert.baumsoft.dev.naturacert.Auditores;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table AUDITORES.
*/
public class AuditoresDao extends AbstractDao<Auditores, Long> {

    public static final String TABLENAME = "AUDITORES";

    /**
     * Properties of entity Auditores.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Usuario = new Property(1, String.class, "usuario", false, "USUARIO");
        public final static Property Nombre = new Property(2, String.class, "nombre", false, "NOMBRE");
        public final static Property Apellido = new Property(3, String.class, "apellido", false, "APELLIDO");
        public final static Property Foto = new Property(4, String.class, "foto", false, "FOTO");
        public final static Property Ref_auditor = new Property(5, Integer.class, "ref_auditor", false, "REF_AUDITOR");
        public final static Property IdToken = new Property(6, long.class, "idToken", false, "ID_TOKEN");
    };

    private DaoSession daoSession;

    private Query<Auditores> tokensBD_Productos_carritoQuery;

    public AuditoresDao(DaoConfig config) {
        super(config);
    }
    
    public AuditoresDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'AUDITORES' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'USUARIO' TEXT," + // 1: usuario
                "'NOMBRE' TEXT," + // 2: nombre
                "'APELLIDO' TEXT," + // 3: apellido
                "'FOTO' TEXT," + // 4: foto
                "'REF_AUDITOR' INTEGER," + // 5: ref_auditor
                "'ID_TOKEN' INTEGER NOT NULL );"); // 6: idToken
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'AUDITORES'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, Auditores entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String usuario = entity.getUsuario();
        if (usuario != null) {
            stmt.bindString(2, usuario);
        }
 
        String nombre = entity.getNombre();
        if (nombre != null) {
            stmt.bindString(3, nombre);
        }
 
        String apellido = entity.getApellido();
        if (apellido != null) {
            stmt.bindString(4, apellido);
        }
 
        String foto = entity.getFoto();
        if (foto != null) {
            stmt.bindString(5, foto);
        }
 
        Integer ref_auditor = entity.getRef_auditor();
        if (ref_auditor != null) {
            stmt.bindLong(6, ref_auditor);
        }
        stmt.bindLong(7, entity.getIdToken());
    }

    @Override
    protected void attachEntity(Auditores entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public Auditores readEntity(Cursor cursor, int offset) {
        Auditores entity = new Auditores( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // usuario
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // nombre
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // apellido
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // foto
            cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5), // ref_auditor
            cursor.getLong(offset + 6) // idToken
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, Auditores entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setUsuario(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setNombre(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setApellido(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setFoto(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setRef_auditor(cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5));
        entity.setIdToken(cursor.getLong(offset + 6));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(Auditores entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(Auditores entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "productos_carrito" to-many relationship of TokensBD. */
    public List<Auditores> _queryTokensBD_Productos_carrito(long idToken) {
        synchronized (this) {
            if (tokensBD_Productos_carritoQuery == null) {
                QueryBuilder<Auditores> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.IdToken.eq(null));
                tokensBD_Productos_carritoQuery = queryBuilder.build();
            }
        }
        Query<Auditores> query = tokensBD_Productos_carritoQuery.forCurrentThread();
        query.setParameter(0, idToken);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getTokensBDDao().getAllColumns());
            builder.append(" FROM AUDITORES T");
            builder.append(" LEFT JOIN TOKENS_BD T0 ON T.'ID_TOKEN'=T0.'_id'");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected Auditores loadCurrentDeep(Cursor cursor, boolean lock) {
        Auditores entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        TokensBD tokensBD = loadCurrentOther(daoSession.getTokensBDDao(), cursor, offset);
         if(tokensBD != null) {
            entity.setTokensBD(tokensBD);
        }

        return entity;    
    }

    public Auditores loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<Auditores> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<Auditores> list = new ArrayList<Auditores>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<Auditores> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<Auditores> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
