package naturacert.baumsoft.dev.naturacert;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import naturacert.baumsoft.dev.naturacert.p2p5;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table P2P5.
*/
public class p2p5Dao extends AbstractDao<p2p5, Long> {

    public static final String TABLENAME = "P2P5";

    /**
     * Properties of entity p2p5.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Nent = new Property(1, Integer.class, "nent", false, "NENT");
        public final static Property Dcpos = new Property(2, Integer.class, "dcpos", false, "DCPOS");
        public final static Property Dsp = new Property(3, Integer.class, "dsp", false, "DSP");
        public final static Property Osv = new Property(4, Integer.class, "osv", false, "OSV");
        public final static Property Sepyn = new Property(5, Integer.class, "sepyn", false, "SEPYN");
        public final static Property Origen = new Property(6, Integer.class, "origen", false, "ORIGEN");
        public final static Property Id_formulario = new Property(7, Integer.class, "id_formulario", false, "ID_FORMULARIO");
        public final static Property Observaciones = new Property(8, String.class, "observaciones", false, "OBSERVACIONES");
    };


    public p2p5Dao(DaoConfig config) {
        super(config);
    }
    
    public p2p5Dao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'P2P5' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'NENT' INTEGER," + // 1: nent
                "'DCPOS' INTEGER," + // 2: dcpos
                "'DSP' INTEGER," + // 3: dsp
                "'OSV' INTEGER," + // 4: osv
                "'SEPYN' INTEGER," + // 5: sepyn
                "'ORIGEN' INTEGER," + // 6: origen
                "'ID_FORMULARIO' INTEGER," + // 7: id_formulario
                "'OBSERVACIONES' TEXT);"); // 8: observaciones
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'P2P5'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, p2p5 entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer nent = entity.getNent();
        if (nent != null) {
            stmt.bindLong(2, nent);
        }
 
        Integer dcpos = entity.getDcpos();
        if (dcpos != null) {
            stmt.bindLong(3, dcpos);
        }
 
        Integer dsp = entity.getDsp();
        if (dsp != null) {
            stmt.bindLong(4, dsp);
        }
 
        Integer osv = entity.getOsv();
        if (osv != null) {
            stmt.bindLong(5, osv);
        }
 
        Integer sepyn = entity.getSepyn();
        if (sepyn != null) {
            stmt.bindLong(6, sepyn);
        }
 
        Integer origen = entity.getOrigen();
        if (origen != null) {
            stmt.bindLong(7, origen);
        }
 
        Integer id_formulario = entity.getId_formulario();
        if (id_formulario != null) {
            stmt.bindLong(8, id_formulario);
        }
 
        String observaciones = entity.getObservaciones();
        if (observaciones != null) {
            stmt.bindString(9, observaciones);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public p2p5 readEntity(Cursor cursor, int offset) {
        p2p5 entity = new p2p5( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // nent
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // dcpos
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3), // dsp
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // osv
            cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5), // sepyn
            cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6), // origen
            cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7), // id_formulario
            cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8) // observaciones
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, p2p5 entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setNent(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setDcpos(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setDsp(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
        entity.setOsv(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setSepyn(cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5));
        entity.setOrigen(cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6));
        entity.setId_formulario(cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7));
        entity.setObservaciones(cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(p2p5 entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(p2p5 entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
