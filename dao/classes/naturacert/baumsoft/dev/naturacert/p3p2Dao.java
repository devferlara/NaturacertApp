package naturacert.baumsoft.dev.naturacert;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import naturacert.baumsoft.dev.naturacert.p3p2;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table P3P2.
*/
public class p3p2Dao extends AbstractDao<p3p2, Long> {

    public static final String TABLENAME = "P3P2";

    /**
     * Properties of entity p3p2.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Niea = new Property(1, Integer.class, "niea", false, "NIEA");
        public final static Property Ieap = new Property(2, Integer.class, "ieap", false, "IEAP");
        public final static Property Rhvs = new Property(3, Integer.class, "rhvs", false, "RHVS");
        public final static Property Phf = new Property(4, Integer.class, "phf", false, "PHF");
        public final static Property Pep = new Property(5, Integer.class, "pep", false, "PEP");
        public final static Property Origen = new Property(6, Integer.class, "origen", false, "ORIGEN");
        public final static Property Id_formulario = new Property(7, Integer.class, "id_formulario", false, "ID_FORMULARIO");
        public final static Property Observaciones = new Property(8, String.class, "observaciones", false, "OBSERVACIONES");
    };


    public p3p2Dao(DaoConfig config) {
        super(config);
    }
    
    public p3p2Dao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'P3P2' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'NIEA' INTEGER," + // 1: niea
                "'IEAP' INTEGER," + // 2: ieap
                "'RHVS' INTEGER," + // 3: rhvs
                "'PHF' INTEGER," + // 4: phf
                "'PEP' INTEGER," + // 5: pep
                "'ORIGEN' INTEGER," + // 6: origen
                "'ID_FORMULARIO' INTEGER," + // 7: id_formulario
                "'OBSERVACIONES' TEXT);"); // 8: observaciones
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'P3P2'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, p3p2 entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer niea = entity.getNiea();
        if (niea != null) {
            stmt.bindLong(2, niea);
        }
 
        Integer ieap = entity.getIeap();
        if (ieap != null) {
            stmt.bindLong(3, ieap);
        }
 
        Integer rhvs = entity.getRhvs();
        if (rhvs != null) {
            stmt.bindLong(4, rhvs);
        }
 
        Integer phf = entity.getPhf();
        if (phf != null) {
            stmt.bindLong(5, phf);
        }
 
        Integer pep = entity.getPep();
        if (pep != null) {
            stmt.bindLong(6, pep);
        }
 
        Integer origen = entity.getOrigen();
        if (origen != null) {
            stmt.bindLong(7, origen);
        }
 
        Integer id_formulario = entity.getId_formulario();
        if (id_formulario != null) {
            stmt.bindLong(8, id_formulario);
        }
 
        String observaciones = entity.getObservaciones();
        if (observaciones != null) {
            stmt.bindString(9, observaciones);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public p3p2 readEntity(Cursor cursor, int offset) {
        p3p2 entity = new p3p2( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // niea
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // ieap
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3), // rhvs
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // phf
            cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5), // pep
            cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6), // origen
            cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7), // id_formulario
            cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8) // observaciones
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, p3p2 entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setNiea(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setIeap(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setRhvs(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
        entity.setPhf(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setPep(cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5));
        entity.setOrigen(cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6));
        entity.setId_formulario(cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7));
        entity.setObservaciones(cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(p3p2 entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(p3p2 entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
