package naturacert.baumsoft.dev.naturacert;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import naturacert.baumsoft.dev.naturacert.p7p4;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table P7P4.
*/
public class p7p4Dao extends AbstractDao<p7p4, Long> {

    public static final String TABLENAME = "P7P4";

    /**
     * Properties of entity p7p4.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Lfdc = new Property(1, Integer.class, "lfdc", false, "LFDC");
        public final static Property Lfpe = new Property(2, Integer.class, "lfpe", false, "LFPE");
        public final static Property Lfcd = new Property(3, Integer.class, "lfcd", false, "LFCD");
        public final static Property Lfau = new Property(4, Integer.class, "lfau", false, "LFAU");
        public final static Property Origen = new Property(5, Integer.class, "origen", false, "ORIGEN");
        public final static Property Obser = new Property(6, String.class, "obser", false, "OBSER");
        public final static Property Id_formulario = new Property(7, Integer.class, "id_formulario", false, "ID_FORMULARIO");
    };


    public p7p4Dao(DaoConfig config) {
        super(config);
    }
    
    public p7p4Dao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'P7P4' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'LFDC' INTEGER," + // 1: lfdc
                "'LFPE' INTEGER," + // 2: lfpe
                "'LFCD' INTEGER," + // 3: lfcd
                "'LFAU' INTEGER," + // 4: lfau
                "'ORIGEN' INTEGER," + // 5: origen
                "'OBSER' TEXT," + // 6: obser
                "'ID_FORMULARIO' INTEGER);"); // 7: id_formulario
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'P7P4'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, p7p4 entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer lfdc = entity.getLfdc();
        if (lfdc != null) {
            stmt.bindLong(2, lfdc);
        }
 
        Integer lfpe = entity.getLfpe();
        if (lfpe != null) {
            stmt.bindLong(3, lfpe);
        }
 
        Integer lfcd = entity.getLfcd();
        if (lfcd != null) {
            stmt.bindLong(4, lfcd);
        }
 
        Integer lfau = entity.getLfau();
        if (lfau != null) {
            stmt.bindLong(5, lfau);
        }
 
        Integer origen = entity.getOrigen();
        if (origen != null) {
            stmt.bindLong(6, origen);
        }
 
        String obser = entity.getObser();
        if (obser != null) {
            stmt.bindString(7, obser);
        }
 
        Integer id_formulario = entity.getId_formulario();
        if (id_formulario != null) {
            stmt.bindLong(8, id_formulario);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public p7p4 readEntity(Cursor cursor, int offset) {
        p7p4 entity = new p7p4( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // lfdc
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // lfpe
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3), // lfcd
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // lfau
            cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5), // origen
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6), // obser
            cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7) // id_formulario
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, p7p4 entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setLfdc(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setLfpe(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setLfcd(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
        entity.setLfau(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setOrigen(cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5));
        entity.setObser(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
        entity.setId_formulario(cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(p7p4 entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(p7p4 entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
