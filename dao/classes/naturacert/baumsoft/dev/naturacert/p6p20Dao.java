package naturacert.baumsoft.dev.naturacert;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import naturacert.baumsoft.dev.naturacert.p6p20;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table P6P20.
*/
public class p6p20Dao extends AbstractDao<p6p20, Long> {

    public static final String TABLENAME = "P6P20";

    /**
     * Properties of entity p6p20.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Fdip = new Property(1, Integer.class, "fdip", false, "FDIP");
        public final static Property Sipd = new Property(2, Integer.class, "sipd", false, "SIPD");
        public final static Property Ecca = new Property(3, Integer.class, "ecca", false, "ECCA");
        public final static Property Mono = new Property(4, Integer.class, "mono", false, "MONO");
        public final static Property Lfbs = new Property(5, Integer.class, "lfbs", false, "LFBS");
        public final static Property Origen = new Property(6, Integer.class, "origen", false, "ORIGEN");
        public final static Property Obser = new Property(7, String.class, "obser", false, "OBSER");
        public final static Property Id_formulario = new Property(8, Integer.class, "id_formulario", false, "ID_FORMULARIO");
    };


    public p6p20Dao(DaoConfig config) {
        super(config);
    }
    
    public p6p20Dao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'P6P20' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'FDIP' INTEGER," + // 1: fdip
                "'SIPD' INTEGER," + // 2: sipd
                "'ECCA' INTEGER," + // 3: ecca
                "'MONO' INTEGER," + // 4: mono
                "'LFBS' INTEGER," + // 5: lfbs
                "'ORIGEN' INTEGER," + // 6: origen
                "'OBSER' TEXT," + // 7: obser
                "'ID_FORMULARIO' INTEGER);"); // 8: id_formulario
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'P6P20'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, p6p20 entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer fdip = entity.getFdip();
        if (fdip != null) {
            stmt.bindLong(2, fdip);
        }
 
        Integer sipd = entity.getSipd();
        if (sipd != null) {
            stmt.bindLong(3, sipd);
        }
 
        Integer ecca = entity.getEcca();
        if (ecca != null) {
            stmt.bindLong(4, ecca);
        }
 
        Integer mono = entity.getMono();
        if (mono != null) {
            stmt.bindLong(5, mono);
        }
 
        Integer lfbs = entity.getLfbs();
        if (lfbs != null) {
            stmt.bindLong(6, lfbs);
        }
 
        Integer origen = entity.getOrigen();
        if (origen != null) {
            stmt.bindLong(7, origen);
        }
 
        String obser = entity.getObser();
        if (obser != null) {
            stmt.bindString(8, obser);
        }
 
        Integer id_formulario = entity.getId_formulario();
        if (id_formulario != null) {
            stmt.bindLong(9, id_formulario);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public p6p20 readEntity(Cursor cursor, int offset) {
        p6p20 entity = new p6p20( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // fdip
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // sipd
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3), // ecca
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // mono
            cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5), // lfbs
            cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6), // origen
            cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7), // obser
            cursor.isNull(offset + 8) ? null : cursor.getInt(offset + 8) // id_formulario
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, p6p20 entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setFdip(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setSipd(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setEcca(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
        entity.setMono(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setLfbs(cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5));
        entity.setOrigen(cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6));
        entity.setObser(cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7));
        entity.setId_formulario(cursor.isNull(offset + 8) ? null : cursor.getInt(offset + 8));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(p6p20 entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(p6p20 entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
