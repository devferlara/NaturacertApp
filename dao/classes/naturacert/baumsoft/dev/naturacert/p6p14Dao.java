package naturacert.baumsoft.dev.naturacert;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import naturacert.baumsoft.dev.naturacert.p6p14;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table P6P14.
*/
public class p6p14Dao extends AbstractDao<p6p14, Long> {

    public static final String TABLENAME = "P6P14";

    /**
     * Properties of entity p6p14.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Lfde = new Property(1, Integer.class, "lfde", false, "LFDE");
        public final static Property Hoap = new Property(2, String.class, "hoap", false, "HOAP");
        public final static Property Semd = new Property(3, Integer.class, "semd", false, "SEMD");
        public final static Property Srrd = new Property(4, String.class, "srrd", false, "SRRD");
        public final static Property Origen = new Property(5, Integer.class, "origen", false, "ORIGEN");
        public final static Property Id_formulario = new Property(6, Integer.class, "id_formulario", false, "ID_FORMULARIO");
        public final static Property Observaciones = new Property(7, String.class, "observaciones", false, "OBSERVACIONES");
    };


    public p6p14Dao(DaoConfig config) {
        super(config);
    }
    
    public p6p14Dao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'P6P14' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'LFDE' INTEGER," + // 1: lfde
                "'HOAP' TEXT," + // 2: hoap
                "'SEMD' INTEGER," + // 3: semd
                "'SRRD' TEXT," + // 4: srrd
                "'ORIGEN' INTEGER," + // 5: origen
                "'ID_FORMULARIO' INTEGER," + // 6: id_formulario
                "'OBSERVACIONES' TEXT);"); // 7: observaciones
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'P6P14'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, p6p14 entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer lfde = entity.getLfde();
        if (lfde != null) {
            stmt.bindLong(2, lfde);
        }
 
        String hoap = entity.getHoap();
        if (hoap != null) {
            stmt.bindString(3, hoap);
        }
 
        Integer semd = entity.getSemd();
        if (semd != null) {
            stmt.bindLong(4, semd);
        }
 
        String srrd = entity.getSrrd();
        if (srrd != null) {
            stmt.bindString(5, srrd);
        }
 
        Integer origen = entity.getOrigen();
        if (origen != null) {
            stmt.bindLong(6, origen);
        }
 
        Integer id_formulario = entity.getId_formulario();
        if (id_formulario != null) {
            stmt.bindLong(7, id_formulario);
        }
 
        String observaciones = entity.getObservaciones();
        if (observaciones != null) {
            stmt.bindString(8, observaciones);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public p6p14 readEntity(Cursor cursor, int offset) {
        p6p14 entity = new p6p14( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // lfde
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // hoap
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3), // semd
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // srrd
            cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5), // origen
            cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6), // id_formulario
            cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7) // observaciones
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, p6p14 entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setLfde(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setHoap(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setSemd(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
        entity.setSrrd(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setOrigen(cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5));
        entity.setId_formulario(cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6));
        entity.setObservaciones(cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(p6p14 entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(p6p14 entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
