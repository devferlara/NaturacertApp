package naturacert.baumsoft.dev.naturacert;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import naturacert.baumsoft.dev.naturacert.p5p10;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table P5P10.
*/
public class p5p10Dao extends AbstractDao<p5p10, Long> {

    public static final String TABLENAME = "P5P10";

    /**
     * Properties of entity p5p10.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Armf = new Property(1, Integer.class, "armf", false, "ARMF");
        public final static Property Pctf = new Property(2, Integer.class, "pctf", false, "PCTF");
        public final static Property Esal = new Property(3, Integer.class, "esal", false, "ESAL");
        public final static Property Ben = new Property(4, Integer.class, "ben", false, "BEN");
        public final static Property Cdel = new Property(5, Integer.class, "cdel", false, "CDEL");
        public final static Property Dtra = new Property(6, Integer.class, "dtra", false, "DTRA");
        public final static Property Fotl = new Property(7, Integer.class, "fotl", false, "FOTL");
        public final static Property Origen = new Property(8, Integer.class, "origen", false, "ORIGEN");
        public final static Property Id_formulario = new Property(9, Integer.class, "id_formulario", false, "ID_FORMULARIO");
        public final static Property Observaciones = new Property(10, String.class, "observaciones", false, "OBSERVACIONES");
    };


    public p5p10Dao(DaoConfig config) {
        super(config);
    }
    
    public p5p10Dao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'P5P10' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'ARMF' INTEGER," + // 1: armf
                "'PCTF' INTEGER," + // 2: pctf
                "'ESAL' INTEGER," + // 3: esal
                "'BEN' INTEGER," + // 4: ben
                "'CDEL' INTEGER," + // 5: cdel
                "'DTRA' INTEGER," + // 6: dtra
                "'FOTL' INTEGER," + // 7: fotl
                "'ORIGEN' INTEGER," + // 8: origen
                "'ID_FORMULARIO' INTEGER," + // 9: id_formulario
                "'OBSERVACIONES' TEXT);"); // 10: observaciones
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'P5P10'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, p5p10 entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer armf = entity.getArmf();
        if (armf != null) {
            stmt.bindLong(2, armf);
        }
 
        Integer pctf = entity.getPctf();
        if (pctf != null) {
            stmt.bindLong(3, pctf);
        }
 
        Integer esal = entity.getEsal();
        if (esal != null) {
            stmt.bindLong(4, esal);
        }
 
        Integer ben = entity.getBen();
        if (ben != null) {
            stmt.bindLong(5, ben);
        }
 
        Integer cdel = entity.getCdel();
        if (cdel != null) {
            stmt.bindLong(6, cdel);
        }
 
        Integer dtra = entity.getDtra();
        if (dtra != null) {
            stmt.bindLong(7, dtra);
        }
 
        Integer fotl = entity.getFotl();
        if (fotl != null) {
            stmt.bindLong(8, fotl);
        }
 
        Integer origen = entity.getOrigen();
        if (origen != null) {
            stmt.bindLong(9, origen);
        }
 
        Integer id_formulario = entity.getId_formulario();
        if (id_formulario != null) {
            stmt.bindLong(10, id_formulario);
        }
 
        String observaciones = entity.getObservaciones();
        if (observaciones != null) {
            stmt.bindString(11, observaciones);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public p5p10 readEntity(Cursor cursor, int offset) {
        p5p10 entity = new p5p10( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // armf
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // pctf
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3), // esal
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // ben
            cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5), // cdel
            cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6), // dtra
            cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7), // fotl
            cursor.isNull(offset + 8) ? null : cursor.getInt(offset + 8), // origen
            cursor.isNull(offset + 9) ? null : cursor.getInt(offset + 9), // id_formulario
            cursor.isNull(offset + 10) ? null : cursor.getString(offset + 10) // observaciones
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, p5p10 entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setArmf(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setPctf(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setEsal(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
        entity.setBen(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setCdel(cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5));
        entity.setDtra(cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6));
        entity.setFotl(cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7));
        entity.setOrigen(cursor.isNull(offset + 8) ? null : cursor.getInt(offset + 8));
        entity.setId_formulario(cursor.isNull(offset + 9) ? null : cursor.getInt(offset + 9));
        entity.setObservaciones(cursor.isNull(offset + 10) ? null : cursor.getString(offset + 10));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(p5p10 entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(p5p10 entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
