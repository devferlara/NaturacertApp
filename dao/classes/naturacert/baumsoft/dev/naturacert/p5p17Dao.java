package naturacert.baumsoft.dev.naturacert;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import naturacert.baumsoft.dev.naturacert.p5p17;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table P5P17.
*/
public class p5p17Dao extends AbstractDao<p5p17, Long> {

    public static final String TABLENAME = "P5P17";

    /**
     * Properties of entity p5p17.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Fgeh = new Property(1, Integer.class, "fgeh", false, "FGEH");
        public final static Property Htevf = new Property(2, Integer.class, "htevf", false, "HTEVF");
        public final static Property Mgaet = new Property(3, Integer.class, "mgaet", false, "MGAET");
        public final static Property Eapf = new Property(4, Integer.class, "eapf", false, "EAPF");
        public final static Property Origen = new Property(5, Integer.class, "origen", false, "ORIGEN");
        public final static Property Id_formulario = new Property(6, Integer.class, "id_formulario", false, "ID_FORMULARIO");
        public final static Property Observaciones = new Property(7, String.class, "observaciones", false, "OBSERVACIONES");
    };


    public p5p17Dao(DaoConfig config) {
        super(config);
    }
    
    public p5p17Dao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'P5P17' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'FGEH' INTEGER," + // 1: fgeh
                "'HTEVF' INTEGER," + // 2: htevf
                "'MGAET' INTEGER," + // 3: mgaet
                "'EAPF' INTEGER," + // 4: eapf
                "'ORIGEN' INTEGER," + // 5: origen
                "'ID_FORMULARIO' INTEGER," + // 6: id_formulario
                "'OBSERVACIONES' TEXT);"); // 7: observaciones
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'P5P17'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, p5p17 entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer fgeh = entity.getFgeh();
        if (fgeh != null) {
            stmt.bindLong(2, fgeh);
        }
 
        Integer htevf = entity.getHtevf();
        if (htevf != null) {
            stmt.bindLong(3, htevf);
        }
 
        Integer mgaet = entity.getMgaet();
        if (mgaet != null) {
            stmt.bindLong(4, mgaet);
        }
 
        Integer eapf = entity.getEapf();
        if (eapf != null) {
            stmt.bindLong(5, eapf);
        }
 
        Integer origen = entity.getOrigen();
        if (origen != null) {
            stmt.bindLong(6, origen);
        }
 
        Integer id_formulario = entity.getId_formulario();
        if (id_formulario != null) {
            stmt.bindLong(7, id_formulario);
        }
 
        String observaciones = entity.getObservaciones();
        if (observaciones != null) {
            stmt.bindString(8, observaciones);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public p5p17 readEntity(Cursor cursor, int offset) {
        p5p17 entity = new p5p17( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // fgeh
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // htevf
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3), // mgaet
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // eapf
            cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5), // origen
            cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6), // id_formulario
            cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7) // observaciones
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, p5p17 entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setFgeh(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setHtevf(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setMgaet(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
        entity.setEapf(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setOrigen(cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5));
        entity.setId_formulario(cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6));
        entity.setObservaciones(cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(p5p17 entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(p5p17 entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
